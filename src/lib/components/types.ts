import { type IconName } from "./icon-types";

export interface IconProps {
  className?: string;
  color?: string;
  maxSize?: string;
  name: IconName;
  size?: string;
  strokeColor?: string;
  strokeWidth?: string;
}
